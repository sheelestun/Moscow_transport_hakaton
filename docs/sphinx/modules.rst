Автодокументация ML-модулей
============================

Ниже — автоматически сгенерированные страницы по docstring-ам исходников
``ml/src/``. Для каждого модуля выводятся все публичные функции, классы
и константы.

.. autosummary::
   :toctree: _autosummary
   :recursive:

   features.tabular
   features.from_stream
   inference_service
   train_catboost
   export_onnx
   metrics_slices
   feature_importance
   verify_streaming
