# Зависимости только для инференс-сервиса (Docker-образ ML). Для обучения — requirements.txt.
# catboost ставится отдельно с --no-deps (см. Dockerfile): его графические зависимости
# (plotly, matplotlib, graphviz, ~150 МБ) для прогноза не нужны; нужное ему перечислено здесь.
numpy>=1.26
pandas>=2.2
scipy>=1.11
six>=1.16
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.7
